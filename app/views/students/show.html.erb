<h1><%= @student.to_s %></h1>
<p><%= "This student is currently #{@student.active ? "active" : "inactive"}." %></p>

<%= link_to activate_student_path(@student) do %>
  <button>Make <%= @student.active ? "Inactive" : "Active" %></button>
<% end %>
